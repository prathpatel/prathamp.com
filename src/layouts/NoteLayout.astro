---
import BaseLayout from "./BaseLayout.astro";
import { formatDate } from "../utils/formatDate";

interface Props {
  frontmatter: {
    title: string;
    date: Date;
    topic?: string;
  };
  slug: string;
}

const { frontmatter, slug } = Astro.props;
const ogImage = `/og/notes/${slug}.png`;
---

<BaseLayout
  title={frontmatter.title}
  image={ogImage}
  type="article"
  showScrollProgress={true}
  publishedTime={frontmatter.date}
  tags={frontmatter.topic ? [frontmatter.topic] : undefined}
>
  <article class="max-w-3xl mx-auto px-4 py-16">
    <header class="mb-10 pb-8 border-b-2 border-gray-900 dark:border-gray-100">
      <div class="flex items-center gap-4 font-mono text-xs uppercase tracking-widest text-gray-500 dark:text-gray-400 mb-5">
        <time datetime={frontmatter.date.toISOString()}>
          {formatDate(frontmatter.date)}
        </time>
        {
          frontmatter.topic && (
            <>
              <span aria-hidden="true">&middot;</span>
              <a
                href={`/notes?topic=${encodeURIComponent(frontmatter.topic)}`}
                class="text-primary-600 dark:text-primary-400 hover:underline underline-offset-4"
              >
                {frontmatter.topic}
              </a>
            </>
          )
        }
      </div>
      <h1 class="font-display text-3xl md:text-5xl font-bold tracking-tighter text-gray-900 dark:text-white">
        {frontmatter.title}
      </h1>
    </header>
    <div
      class="prose prose-gray dark:prose-invert max-w-none prose-headings:font-semibold prose-a:text-primary-600 dark:prose-a:text-primary-400 prose-code:text-primary-700 dark:prose-code:text-primary-300 prose-code:bg-gray-100 dark:prose-code:bg-gray-800 prose-code:px-1.5 prose-code:py-0.5 prose-code:rounded"
    >
      <slot />
    </div>
  </article>
</BaseLayout>
